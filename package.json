{
  "name": "clipchamp-webrtc-chat",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "install:both": "npm install && cd angular-client && npm install",
    "build:server": "tsc",
    "build:client": "cd angular-client && ng build",
    "build": "npm run build:server && npm run build:client",
    "build:start": "npm run build && npm start",
    "start": "node bin/www",
    "test": "cd angular-client && npm run test:ci"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.11.1",
    "@types/lodash": "^4.14.106",
    "@types/node": "^9.6.0",
    "@types/ws": "^4.0.2"
  },
  "dependencies": {
    "express": "^4.16.3",
    "lodash": "^4.17.5",
    "ws": "^5.1.0"
  }
}
